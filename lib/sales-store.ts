"use client";
import type {Currency} from "./currency";
export type Sale={id:string;saleId:string;vehicleId:string;stockId:string;vehicleName:string;customerId:string;customerName:string;saleDate:string;sellingPrice:number;amountPaid:number;balance:number;vehicleCost:number;profit:number;currency?:Currency;mixedCurrency?:boolean;exchangeRate?:number;originalSellingPrice?:number;originalAmountPaid?:number;originalBalance?:number;sellingPriceZmw?:number;amountPaidZmw?:number;balanceZmw?:number;paymentMethod:string;status:"Partially Paid"|"Fully Paid";createdAt:string};
const KEY="covenant-motors-sales";
export function loadSales():Sale[]{if(typeof window==="undefined")return[];const raw=localStorage.getItem(KEY);if(!raw)return[];try{return (JSON.parse(raw) as any[]).map(x=>{const selling=Number(x.originalSellingPrice??x.sellingPrice??0);const paid=Number(x.originalAmountPaid??x.amountPaid??0);return {...x,currency:x.currency||"ZMW",sellingPrice:selling,amountPaid:paid,balance:Math.max(0,selling-paid),profit:Number(x.profit??0)} as Sale;});}catch{return[];}}
export function saveSales(items:Sale[]){localStorage.setItem(KEY,JSON.stringify(items));}
export function nextSaleId(items:Sale[]){const year=new Date().getFullYear();const nums=items.filter(x=>x.saleId.startsWith(`SAL-${year}-`)).map(x=>Number(x.saleId.split("-")[2])).filter(Number.isFinite);return `SAL-${year}-${String(Math.max(0,...nums)+1).padStart(4,"0")}`;}
