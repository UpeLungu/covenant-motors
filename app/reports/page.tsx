"use client";
import {useEffect,useMemo,useState} from "react";
import {Download,FileBarChart2,Printer} from "lucide-react";
import {loadVehicles} from "@/lib/vehicle-store";import {loadSales} from "@/lib/sales-store";import {loadTrips,tripSpent} from "@/lib/operations-store";import {loadVehicleExpenses} from "@/lib/ledger-store";import {loadFinanceEntries} from "@/lib/finance-store";import {loadTaxReturns} from "@/lib/tax-store";import {formatCurrency,formatZmw} from "@/lib/currency";
type Report="vehicle-profit"|"sales"|"stock"|"cashbook"|"customers"|"suppliers"|"drivers"|"tax";type Row=Record<string,string|number>;const titles:Record<Report,string>={"vehicle-profit":"Vehicle Profit","sales":"Sales Register","stock":"Vehicle Stock","cashbook":"Cash Book","customers":"Customer Balances","suppliers":"Supplier Balances","drivers":"Driver Performance","tax":"Tax Returns"};
export default function ReportsPage(){const [report,setReport]=useState<Report>("vehicle-profit"),[vehicles,setVehicles]=useState<any[]>([]),[sales,setSales]=useState<any[]>([]),[trips,setTrips]=useState<any[]>([]),[expenses,setExpenses]=useState<any[]>([]),[finance,setFinance]=useState<any[]>([]),[tax,setTax]=useState<any[]>([]);useEffect(()=>{setVehicles(loadVehicles());setSales(loadSales());setTrips(loadTrips());setExpenses(loadVehicleExpenses());setFinance(loadFinanceEntries());setTax(loadTaxReturns())},[]);
 const rows=useMemo<Row[]>(()=>{if(report==="vehicle-profit")return sales.map(s=>({"Stock ID":s.stockId,Vehicle:s.vehicleName,Customer:s.customerName,"Sale Currency":s.currency||"ZMW","Original Selling Price":formatCurrency(s.originalSellingPrice??s.sellingPrice,s.currency||"ZMW"),"Selling Price ZMW":s.sellingPrice,"Landed Cost ZMW":s.vehicleCost,"Profit ZMW":s.profit,Status:s.status}));if(report==="sales")return sales.map(s=>({"Sale ID":s.saleId,Date:s.saleDate,"Stock ID":s.stockId,Customer:s.customerName,Currency:s.currency||"ZMW","Original Sale":formatCurrency(s.originalSellingPrice??s.sellingPrice,s.currency||"ZMW"),"Selling Price ZMW":s.sellingPrice,"Paid ZMW":s.amountPaid,"Balance ZMW":s.balance,"Profit ZMW":s.profit,Status:s.status}));if(report==="stock")return vehicles.map(v=>({"Stock ID":v.stockId,Vehicle:`${v.make} ${v.model} ${v.year}`,VIN:v.vin,Supplier:v.supplier,Location:v.currentLocation,"Purchase Currency":v.purchaseCurrency||"ZMW","Original Purchase":formatCurrency(v.originalPurchasePrice??v.purchasePrice,v.purchaseCurrency||"ZMW"),"Purchase Price ZMW":v.purchasePrice,"Total Cost ZMW":v.totalCost,Status:v.status}));if(report==="cashbook")return finance.map(x=>({Date:x.date,Reference:x.reference||"",Type:x.type,Description:x.description,Currency:x.currency||"ZMW","Money In ZMW":x.moneyInZmw??x.moneyIn,"Money Out ZMW":x.moneyOutZmw??x.moneyOut}));if(report==="customers")return sales.map(s=>({Customer:s.customerName,"Sale ID":s.saleId,Vehicle:s.stockId,Currency:s.currency||"ZMW","Selling Price ZMW":s.sellingPrice,"Paid ZMW":s.amountPaid,"Balance ZMW":s.balance,Status:s.status}));if(report==="suppliers")return vehicles.map(v=>{const paid=finance.filter(x=>x.type==="Supplier Payment"&&x.relatedId===v.id).reduce((a:number,b:any)=>a+Number(b.moneyOutZmw??b.moneyOut??0),0);return {Supplier:v.supplier,"Stock ID":v.stockId,Vehicle:`${v.make} ${v.model}`,"Purchase Price ZMW":v.purchasePrice,"Paid ZMW":paid,"Balance ZMW":Math.max(0,v.purchasePrice-paid)}});if(report==="drivers")return trips.map(t=>({Driver:t.driverName,"Trip ID":t.tripId,Vehicle:t.vehicleStockId,Route:`${t.collectionPoint} to ${t.destination}`,"Advance ZMW":t.advanceZmw??t.advance,"Expenses ZMW":tripSpent(t),"Balance ZMW":(t.advanceZmw??t.advance)-tripSpent(t),Status:t.status}));return tax.map(t=>({Type:t.type,Period:t.period,"Taxable Amount ZMW":t.taxableAmount,"Tax Due ZMW":t.taxDue,"Due Date":t.dueDate,Status:t.status,Reference:t.reference||""}))},[report,vehicles,sales,trips,expenses,finance,tax]);
 const totals=useMemo(()=>({records:rows.length,sales:sales.reduce((a,b)=>a+Number(b.sellingPrice??0),0),profit:sales.reduce((a,b)=>a+Number(b.profit??0),0),outstanding:sales.reduce((a,b)=>a+Number(b.balance??0),0)}),[rows,sales]);function exportCsv(){if(!rows.length)return alert("There is no data to export.");const headers=Object.keys(rows[0]);const csv=[headers.join(","),...rows.map(r=>headers.map(h=>`"${String(r[h]??"").replaceAll('"','""')}"`).join(","))].join("\n");const blob=new Blob([csv],{type:"text/csv;charset=utf-8"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=`covenant-motors-${report}.csv`;a.click();URL.revokeObjectURL(url)}const headers=rows.length?Object.keys(rows[0]):[];const isMoney=(h:string)=>h.includes("ZMW")||h==="Profit";
 return <><div className="topbar"><div className="title"><h1>Reports & Analytics</h1><p>Original currencies are preserved; consolidated management reporting is in ZMW.</p></div><div className="quick-actions"><button className="button secondary" onClick={()=>window.print()}><Printer size={17}/>Print</button><button className="button" onClick={exportCsv}><Download size={17}/>Export CSV</button></div></div><div className="cards"><div className="card metric"><div><div className="metric-label">Report Records</div><div className="metric-value">{totals.records}</div></div><div className="metric-icon"><FileBarChart2/></div></div><div className="card"><div className="metric-label">Sales Revenue (ZMW)</div><div className="metric-value small-money">{formatZmw(totals.sales)}</div></div><div className="card"><div className="metric-label">Gross Profit (ZMW)</div><div className="metric-value small-money">{formatZmw(totals.profit)}</div></div><div className="card"><div className="metric-label">Outstanding (ZMW)</div><div className="metric-value small-money">{formatZmw(totals.outstanding)}</div></div></div><div className="deal-tabs">{(Object.keys(titles) as Report[]).map(key=><button key={key} className={report===key?"active":""} onClick={()=>setReport(key)}>{titles[key]}</button>)}</div><section className="panel report-print"><div className="panel-head"><div><h2>{titles[report]} Report</h2><small className="muted">Generated {new Date().toLocaleString("en-ZM")}</small></div><span className="badge available">{rows.length} records</span></div><div className="table-wrap"><table><thead><tr>{headers.map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i}>{headers.map(h=><td key={h} className={h.includes("Profit")&&Number(r[h])<0?"loss":h.includes("Profit")?"profit":""}>{isMoney(h)&&typeof r[h]==="number"?formatZmw(Number(r[h])):String(r[h]??"")}</td>)}</tr>)}</tbody></table>{!rows.length&&<div className="empty">No data is available for this report yet.</div>}</div></section></>}
