"use client";
import Link from "next/link";
import {FormEvent,useEffect,useMemo,useState} from "react";
import {Car,ChevronRight,FileText,Filter,MapPin,PenLine,Plus,Search,ShoppingCart,Wallet,X} from "lucide-react";
import {loadVehicles,saveVehicles} from "@/lib/vehicle-store";
import {Vehicle} from "@/lib/types";
import {Currency,formatCurrency,normalizeCurrency,toZmw} from "@/lib/currency";
type SortOption="newest"|"stock"|"vehicle"|"value-high"|"value-low";
export default function VehiclesPage(){
 const [vehicles,setVehicles]=useState<Vehicle[]>([]),[query,setQuery]=useState(""),[status,setStatus]=useState("All"),[currency,setCurrency]=useState("All"),[sort,setSort]=useState<SortOption>("newest"),[editing,setEditing]=useState<Vehicle|null>(null),[notice,setNotice]=useState("");
 useEffect(()=>setVehicles(loadVehicles()),[]);useEffect(()=>{if(!notice)return;const timer=setTimeout(()=>setNotice(""),2800);return()=>clearTimeout(timer)},[notice]);
 const valueZmw=(v:Vehicle)=>Number(v.estimatedSellingPriceZmw??toZmw(Number(v.estimatedSellingPrice||0),v.estimatedSellingCurrency||"ZMW",v.estimatedSellingExchangeRate));
 const purchaseZmw=(v:Vehicle)=>Number(v.purchasePriceZmw??toZmw(Number((v.originalPurchasePrice??v.purchasePrice)??0),v.purchaseCurrency||"ZMW",v.purchaseExchangeRate));
 const filtered=useMemo(()=>vehicles.filter(v=>{const text=`${v.stockId} ${v.vin} ${v.registrationNumber||""} ${v.make} ${v.model} ${v.supplier} ${v.currentLocation}`.toLowerCase();return text.includes(query.toLowerCase())&&(status==="All"||v.status===status)&&(currency==="All"||normalizeCurrency(v.estimatedSellingCurrency)===currency)}).sort((a,b)=>{if(sort==="stock")return a.stockId.localeCompare(b.stockId);if(sort==="vehicle")return `${a.make} ${a.model}`.localeCompare(`${b.make} ${b.model}`);if(sort==="value-high")return valueZmw(b)-valueZmw(a);if(sort==="value-low")return valueZmw(a)-valueZmw(b);return new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime()}),[vehicles,query,status,currency,sort]);
 const activeStock=vehicles.filter(v=>v.status!=="Sold"),available=vehicles.filter(v=>v.status==="Available").length,transit=vehicles.filter(v=>v.status==="In Transit").length,stockValue=activeStock.reduce((s,v)=>s+valueZmw(v),0);const badge=(s:string)=>s==="Available"?"available":s==="In Transit"?"transit":s==="Sold"?"sold":"awaiting";
 function saveValuation(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!editing)return;const f=new FormData(e.currentTarget),estimatedSellingPrice=Number(f.get("estimatedSellingPrice")||0),estimatedSellingCurrency=String(f.get("estimatedSellingCurrency")||"ZMW") as Currency,estimatedSellingExchangeRate=estimatedSellingCurrency==="USD"?Number(f.get("estimatedSellingExchangeRate")||0):1;if(estimatedSellingCurrency==="USD"&&estimatedSellingExchangeRate<=0)return;const estimatedSellingPriceZmw=toZmw(estimatedSellingPrice,estimatedSellingCurrency,estimatedSellingExchangeRate);const next=vehicles.map(v=>v.id===editing.id?{...v,estimatedSellingPrice,estimatedSellingCurrency,estimatedSellingExchangeRate,estimatedSellingPriceZmw}:v);saveVehicles(next);setVehicles(next);setNotice(`${editing.stockId} valuation updated`);setEditing(null)}
 const clearFilters=()=>{setQuery("");setStatus("All");setCurrency("All");setSort("newest")};
 return <>{notice&&<div className="app-toast"><span>✓</span>{notice}</div>}<div className="page-heading"><div><span className="eyebrow dark">INVENTORY MANAGEMENT</span><h1>Vehicle Stock</h1><p>Manage vehicle records, original transaction values and ZMW accounting valuations.</p></div><Link className="button" href="/vehicles/new"><Plus size={18}/>Add Vehicle</Link></div><div className="inventory-summary"><div className="mini-stat"><Car/><span><small>Total vehicles</small><b>{vehicles.length}</b></span></div><div className="mini-stat"><MapPin/><span><small>Available</small><b>{available}</b></span></div><div className="mini-stat"><Car/><span><small>In transit</small><b>{transit}</b></span></div><div className="mini-stat dual-value-stat"><Wallet/><span><small>Estimated stock value</small><b>{formatCurrency(stockValue,"ZMW")}</b></span></div></div>{editing&&<form className="form-card section-gap professional-form valuation-editor" onSubmit={saveValuation}><div className="form-heading"><div><span className="eyebrow dark">STOCK VALUATION</span><h2>{editing.stockId} · {editing.make} {editing.model}</h2><p>Keep the quoted amount while storing its ZMW accounting equivalent.</p></div><button type="button" className="icon-button" onClick={()=>setEditing(null)}><X size={18}/></button></div><div className="form-grid"><div className="field"><label>Quoted currency</label><select className="select" name="estimatedSellingCurrency" defaultValue={normalizeCurrency(editing.estimatedSellingCurrency)}><option value="ZMW">Zambian Kwacha (ZMW)</option><option value="USD">US Dollar (USD)</option></select></div><div className="field"><label>Estimated selling price</label><input className="input" name="estimatedSellingPrice" type="number" min="0" step="0.01" defaultValue={editing.estimatedSellingPrice||0} required/></div><div className="field"><label>USD to ZMW rate (only for USD)</label><input className="input" name="estimatedSellingExchangeRate" type="number" min="0" step="0.0001" defaultValue={editing.estimatedSellingExchangeRate||1}/></div></div><div className="actions"><button type="button" className="button secondary" onClick={()=>setEditing(null)}>Cancel</button><button className="button">Save Valuation</button></div></form>}<section className="panel inventory-panel"><div className="inventory-toolbar advanced-toolbar"><div className="search-control"><Search size={18}/><input placeholder="Search stock ID, VIN, vehicle, supplier or location" value={query} onChange={e=>setQuery(e.target.value)}/></div><div className="filter-control"><Filter size={17}/><select value={status} onChange={e=>setStatus(e.target.value)}><option>All</option><option>Awaiting Collection</option><option>In Transit</option><option>Available</option><option>Reserved</option><option>Under Repair</option><option>Sold</option></select></div><div className="filter-control"><select value={currency} onChange={e=>setCurrency(e.target.value)}><option value="All">All quoted currencies</option><option value="ZMW">ZMW quotes</option><option value="USD">USD quotes</option></select></div><div className="filter-control"><select value={sort} onChange={e=>setSort(e.target.value as SortOption)}><option value="newest">Newest first</option><option value="stock">Stock ID</option><option value="vehicle">Vehicle name</option><option value="value-high">Highest ZMW value</option><option value="value-low">Lowest ZMW value</option></select></div><span className="result-count">{filtered.length} of {vehicles.length}</span>{(query||status!=="All"||currency!=="All"||sort!=="newest")&&<button className="clear-filter" type="button" onClick={clearFilters}>Clear</button>}</div><div className="desktop-stock-table table-wrap"><table><thead><tr><th>Stock ID</th><th>Vehicle</th><th>Status</th><th>Accounting Cost</th><th>Estimated Value</th><th>Actions</th></tr></thead><tbody>{filtered.map(v=>{const pc=normalizeCurrency(v.purchaseCurrency),vc=normalizeCurrency(v.estimatedSellingCurrency);return <tr key={v.id}><td><Link className="stock-link" href={`/vehicles/${v.id}`}>{v.stockId}</Link><small>{v.registrationNumber||"Not registered"}</small></td><td><div className="vehicle-cell"><div className="vehicle-thumb"><Car size={19}/></div><span><b>{v.make} {v.model}</b><small>{v.year} · {v.currentLocation}</small></span></div></td><td><span className={`badge ${badge(v.status)}`}>{v.status}</span></td><td><b>{formatCurrency(purchaseZmw(v),"ZMW")}</b>{pc==="USD"&&<small>Original {formatCurrency(Number(v.originalPurchasePrice??v.purchasePrice),"USD")}</small>}</td><td><b>{v.estimatedSellingPrice?formatCurrency(valueZmw(v),"ZMW"):"Not valued"}</b>{v.estimatedSellingPrice&&vc==="USD"&&<small>Quoted {formatCurrency(v.estimatedSellingPrice,"USD")}</small>}</td><td><div className="row-actions"><button className="button secondary compact-button" type="button" onClick={()=>setEditing(v)}><PenLine size={14}/>Value</button><Link className="button secondary compact-button" href={`/vehicles/${v.id}`}><FileText size={14}/>Deal Jacket</Link>{v.status!=="Sold"&&<Link className="row-action" href="/sales"><ShoppingCart size={16}/></Link>}<Link className="row-action" href={`/vehicles/${v.id}`}><ChevronRight size={18}/></Link></div></td></tr>})}</tbody></table></div><div className="mobile-stock-list">{filtered.map(v=>{const pc=normalizeCurrency(v.purchaseCurrency),vc=normalizeCurrency(v.estimatedSellingCurrency);return <article className="mobile-stock-card" key={v.id}><Link href={`/vehicles/${v.id}`} className="mobile-stock-top"><div className="vehicle-thumb"><Car size={19}/></div><div><b>{v.make} {v.model} {v.year}</b><small>{v.stockId} · {v.registrationNumber||"Not registered"}</small></div><ChevronRight size={18}/></Link><div className="mobile-stock-meta"><span><small>Status</small><b className={`badge ${badge(v.status)}`}>{v.status}</b></span><span><small>Accounting cost</small><b>{formatCurrency(purchaseZmw(v),"ZMW")}</b>{pc==="USD"&&<small>{formatCurrency(Number(v.originalPurchasePrice??v.purchasePrice),"USD")}</small>}</span><span><small>Estimated value</small><b>{v.estimatedSellingPrice?formatCurrency(valueZmw(v),"ZMW"):"Not valued"}</b>{v.estimatedSellingPrice&&vc==="USD"&&<small>{formatCurrency(v.estimatedSellingPrice,"USD")}</small>}</span></div><div className="mobile-card-actions"><button className="button secondary compact-button" type="button" onClick={()=>setEditing(v)}><PenLine size={15}/>Edit Value</button><Link className="button secondary compact-button" href={`/vehicles/${v.id}`}><FileText size={15}/>Deal Jacket</Link></div></article>})}</div>{filtered.length===0&&<div className="empty"><Search size={32}/><h3>No matching vehicles</h3><p>Change the search or filters to view other stock.</p><button className="button secondary" type="button" onClick={clearFilters}>Reset filters</button></div>}</section></>;
}
